extends ../../../layouts/fullWidth.pug

block page-header
  h2 Module Delete

block content
  .row.mt-5
      .col-md-6.m-auto
          .card.card-body
            p Do you really want to delete: 
              strong #{module.name}
            form(method="POST")
              button(type="submit").btn.btn-block.btn-warning Yes
              a(href="/api/modules").btn.btn-block.btn-primary No